# Example typescript web app project

## Commands

To get started run in this directory:

```bash
npm i
```

to install the necessary nodejs packages.

To build your code run:

```bash
npm run build
```

To run a local web server run:

```bash
npm run serve
```

than go to http://localhost:8080 to see your web app.
